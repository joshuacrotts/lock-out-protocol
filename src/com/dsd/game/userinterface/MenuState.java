package com.dsd.game.userinterface;

/**
 * Enum describing what state the menu is on.
 *
 * @author Joshua
 */
public enum MenuState {
    MAIN, DIFFICULTY, HELP, LOGIN, OPTIONS, RESOLUTION, PLAYER_GENDER, VOLUME, AUDIO;
}
