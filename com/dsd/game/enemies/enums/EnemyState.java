package com.dsd.game.enemies.enums;

/**
 * This is an enum defining the possible states that an enemy can be in. For
 * now, these are left alone.
 *
 * @author Joshua
 */
public enum EnemyState {
    WALKING, ATTACKING, DEAD;
}
